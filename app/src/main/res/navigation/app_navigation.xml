<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_capsule_list">

    <!-- Capsule List Fragment -->
    <fragment
        android:id="@+id/nav_capsule_list"
        android:name="com.ideasapp.lovetimecapsule.presentation.ListCapsuleFragment"
        android:label="@string/list" />

    <!-- Add Capsule Fragment -->
    <fragment
        android:id="@+id/nav_add_capsule"
        android:name="com.ideasapp.lovetimecapsule.presentation.AddCapsuleFragment"
        android:label="@string/add_capsule" />

    <!-- Show Capsule Fragment -->
    <fragment
        android:id="@+id/show_capsule"
        android:name="com.ideasapp.lovetimecapsule.presentation.ShowCapsuleFragment"
        android:label="@string/opened_capsule" />
</navigation>